/**
 * Function Calls
 * Bianca Granata
 * 
 * working on the Function calls examples.
 */

"use strict";


function setup() {
    // happens immediately when starting the program, at once
    createCanvas(640, 480); //will create a canvas on a page where we can draw stuff using p5
}


function draw() {
    // every frame that the program is on how it works with time
    background(255, 100, 100); //it starts with the name of a function and then the arguments
    rect(200, 80, 240, 320);
}